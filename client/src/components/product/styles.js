import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles((theme) => ({
  root: {
    height: '35rem',
    width: '17rem',
  },

  cardMedia: {
    margin: '2vh',
    width: '15rem',
    height: '22rem',
  },
}))
